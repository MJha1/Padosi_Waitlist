{
  "name": "padosi-waitlist",
  "version": "1.0.0",
  "private": true,
  "description": "Padosi waitlist landing page + lightweight lead capture (Railway)",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
