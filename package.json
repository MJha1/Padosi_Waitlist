{
  "name": "padosi-waitlist",
  "version": "1.0.0",
  "private": true,
  "description": "Padosi waitlist landing page (static site served on Railway)",
  "scripts": {
    "start": "serve -l tcp://0.0.0.0:${PORT:-3000} ."
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
